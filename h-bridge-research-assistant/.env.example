# 로컬 개발용 — 복사: .env.example → .env (절대 Git push 금지)
# Cloud(share.streamlit.io) 배포는 루트 app.py 기준 → Secrets 에 GEMINI_API_KEY 설정

GEMINI_API_KEY=your_gemini_api_key_here
GOOGLE_SHEET_ID=your_google_sheet_id_here
