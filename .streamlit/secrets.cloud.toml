# Streamlit Cloud → Settings → Secrets 에 붙여넣기 (값만 채우기)
# Git 커밋 금지 — .gitignore 에 secrets.toml 만 등록됨

UPSTAGE_API_KEY = ""
UPSTAGE_MODEL = "solar-pro3"
LLM_PROVIDER = "upstage"

[supabase]
url = ""
key = ""

# GEMINI_API_KEY = ""
# GEMINI_MODEL = "gemini-2.5-flash"
# LLM_PROVIDER = "gemini"
